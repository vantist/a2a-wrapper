{
  "$ref": "#/definitions/AgentConfig",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AgentCardConfig": {
      "additionalProperties": false,
      "description": "Agent identity and capabilities advertised via the A2A Agent Card.",
      "properties": {
        "defaultInputModes": {
          "description": "Supported input modes (default: [\"text\"])",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "defaultOutputModes": {
          "description": "Supported output modes (default: [\"text\"])",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "description": {
          "description": "Agent description (shown to orchestrators / callers)",
          "type": "string"
        },
        "name": {
          "description": "Human-readable agent name",
          "type": "string"
        },
        "protocolVersion": {
          "description": "Protocol version (default: \"0.3.0\")",
          "type": "string"
        },
        "provider": {
          "additionalProperties": false,
          "description": "Agent provider info",
          "properties": {
            "organization": {
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          },
          "required": [
            "organization"
          ],
          "type": "object"
        },
        "pushNotifications": {
          "description": "Enable push notifications (default: false)",
          "type": "boolean"
        },
        "skills": {
          "description": "Skills this agent exposes",
          "items": {
            "$ref": "#/definitions/SkillConfig"
          },
          "type": "array"
        },
        "stateTransitionHistory": {
          "deprecated": "This capability is not implemented in the A2A v1.0 spec and\nwas removed. Kept for backward compatibility only — value is ignored.\nDefault: false.",
          "description": "Enable state transition history capability advertisement.",
          "type": "boolean"
        },
        "streaming": {
          "description": "Enable streaming capability (default: true)",
          "type": "boolean"
        },
        "version": {
          "description": "Agent software version (default: \"1.0.0\")",
          "type": "string"
        }
      },
      "required": [
        "name",
        "description"
      ],
      "type": "object"
    },
    "AgentConfig": {
      "additionalProperties": false,
      "description": "Complete agent configuration.\n\nThis is what a JSON config file (e.g. `agents/example/config.json`) maps to. All fields are optional — sensible defaults are applied for everything.",
      "properties": {
        "agentCard": {
          "$ref": "#/definitions/AgentCardConfig",
          "description": "Agent card identity & capabilities"
        },
        "configDir": {
          "description": "Directory containing the agent's config.json file. Populated automatically by the CLI scaffold when a config file path is provided. Used by the materializer for resolving relative paths in memory config.",
          "type": "string"
        },
        "events": {
          "description": "Event transport configuration for sideband observability events."
        },
        "features": {
          "$ref": "#/definitions/FeatureFlags",
          "description": "Feature flags"
        },
        "logging": {
          "$ref": "#/definitions/LoggingConfig",
          "description": "Logging settings"
        },
        "mcp": {
          "additionalProperties": {
            "$ref": "#/definitions/McpServerConfig"
          },
          "description": "MCP servers to register with OpenCode at startup. Keys are server names, values are local or remote configs.",
          "type": "object"
        },
        "memory": {
          "description": "Optional memory configuration for persisting instructions and skills. When present, the materializer writes these to backend-specific paths in the workspace directory during executor initialization."
        },
        "opencode": {
          "$ref": "#/definitions/OpenCodeConfig",
          "description": "OpenCode connection settings"
        },
        "server": {
          "$ref": "#/definitions/ServerConfig",
          "description": "Network / server settings"
        },
        "session": {
          "$ref": "#/definitions/SessionConfig",
          "description": "Session management"
        },
        "subAgents": {
          "description": "Optional sub-agents to expose as MCP tools via the `a2a-mcp-skillmap` bridge. When present, the parent agent spawns skillmap as a stdio MCP server and registers it under the reserved `a2a-subagents` key in the resolved  {@link  mcp }  map. Sub-agents declared here become callable as ordinary MCP tools by the OpenCode runtime.\n\nWhen this field is absent or `agents` is empty, the parent skips every sub-agent code path with no side effects."
        },
        "timeouts": {
          "$ref": "#/definitions/TimeoutConfig",
          "description": "Timeout settings"
        }
      },
      "required": [
        "agentCard"
      ],
      "type": "object"
    },
    "FeatureFlags": {
      "additionalProperties": false,
      "description": "Feature toggles for runtime behavior.",
      "properties": {
        "autoAnswerQuestions": {
          "description": "Auto-answer question requests (default: true)",
          "type": "boolean"
        },
        "autoApprovePermissions": {
          "description": "Auto-approve all permission requests (default: true)",
          "type": "boolean"
        },
        "enablePollingFallback": {
          "description": "Enable polling fallback when SSE fails (default: true)",
          "type": "boolean"
        },
        "streamArtifactChunks": {
          "description": "Stream artifact chunks individually (A2A spec-correct) vs buffer and send single artifact (inspector-compatible). Default: false (buffered).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "LoggingConfig": {
      "additionalProperties": false,
      "description": "Logging settings.",
      "properties": {
        "level": {
          "description": "Log level: \"debug\" | \"info\" | \"warn\" | \"error\" (default: \"info\")",
          "type": "string"
        }
      },
      "type": "object"
    },
    "McpLocalServerConfig": {
      "additionalProperties": false,
      "description": "Configuration for a local (stdio) MCP server.",
      "properties": {
        "command": {
          "description": "Command and arguments to launch the server. Values support `${ENV_VAR}` (or `$ENV_VAR`) substitution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "description": "Enable on startup (default: true)",
          "type": "boolean"
        },
        "environment": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Environment variables passed to the spawned process. Values support `${ENV_VAR}` (or `$ENV_VAR`) substitution so secrets stay out of config.json — e.g. `{ \"API_KEY\": \"${MY_API_KEY}\" }`.",
          "type": "object"
        },
        "timeout": {
          "description": "Request timeout in ms (default: 5000)",
          "type": "number"
        },
        "type": {
          "const": "local",
          "type": "string"
        }
      },
      "required": [
        "type",
        "command"
      ],
      "type": "object"
    },
    "McpOAuthServerConfig": {
      "additionalProperties": false,
      "description": "OAuth config for remote MCP servers that require OAuth2 authentication.",
      "properties": {
        "clientId": {
          "description": "OAuth client ID. Omit to attempt dynamic client registration (RFC 7591).",
          "type": "string"
        },
        "clientSecret": {
          "description": "OAuth client secret (if required by the authorization server)",
          "type": "string"
        },
        "scope": {
          "description": "OAuth scopes to request during authorization",
          "type": "string"
        }
      },
      "type": "object"
    },
    "McpRemoteServerConfig": {
      "additionalProperties": false,
      "description": "Configuration for a remote (HTTP/SSE) MCP server.",
      "properties": {
        "enabled": {
          "description": "Enable on startup (default: true)",
          "type": "boolean"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra headers sent with every request (e.g. { \"Authorization\": \"Bearer <token>\" }). Values support `${ENV_VAR}` (or `$ENV_VAR`) substitution so secrets stay out of config.json — e.g. `{ \"Authorization\": \"Bearer ${LINEAR_API_KEY}\" }`.",
          "type": "object"
        },
        "oauth": {
          "anyOf": [
            {
              "$ref": "#/definitions/McpOAuthServerConfig"
            },
            {
              "const": false,
              "type": "boolean"
            }
          ],
          "description": "OAuth authentication config. Set to false to disable OAuth auto-detection."
        },
        "timeout": {
          "description": "Request timeout in ms (default: 5000)",
          "type": "number"
        },
        "type": {
          "const": "remote",
          "type": "string"
        },
        "url": {
          "description": "URL of the remote MCP server (e.g. http://127.0.0.1:8080/sse)",
          "type": "string"
        }
      },
      "required": [
        "type",
        "url"
      ],
      "type": "object"
    },
    "McpServerConfig": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpLocalServerConfig"
        },
        {
          "$ref": "#/definitions/McpRemoteServerConfig"
        }
      ],
      "description": "Union of local and remote MCP server configs."
    },
    "OpenCodeConfig": {
      "additionalProperties": false,
      "description": "How the wrapper connects to and interacts with OpenCode.",
      "properties": {
        "agent": {
          "description": "Default agent (e.g. \"code-assistant\")",
          "type": "string"
        },
        "baseUrl": {
          "description": "OpenCode server base URL (default: \"http://localhost:4096\")",
          "type": "string"
        },
        "contextFile": {
          "description": "Filename for the pre-built domain context file in the workspace directory. Default: \"context.md\"",
          "type": "string"
        },
        "contextPrompt": {
          "description": "Default prompt sent to OpenCode to build the domain context file. The LLM will write its findings to the contextFile in the workspace. Agent-specific: e.g. \"Explore the workspace, list all files, summarise what each module does.\"",
          "type": "string"
        },
        "model": {
          "description": "Default model (e.g. \"github-copilot/claude-sonnet-4.5\")",
          "type": "string"
        },
        "projectDirectory": {
          "description": "Target project directory for all OpenCode API calls",
          "type": "string"
        },
        "systemPrompt": {
          "description": "System prompt prepended to the first message of every session. Use this to give the LLM its persona, role, and behavioral rules.",
          "type": "string"
        },
        "systemPromptMode": {
          "description": "How the system prompt is applied when injected into the first user message.\n\n- \"append\" (default): system prompt is injected as-is before the first user message.\n\n- \"replace\": a production-ready preamble is automatically prepended to your prompt   that prevents tool/internals disclosure and enforces clean agent persona.   Use this for production deployments.",
          "enum": [
            "append",
            "replace"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "ServerConfig": {
      "additionalProperties": false,
      "description": "Network and server settings.",
      "properties": {
        "advertiseHost": {
          "description": "Hostname advertised in agent card URLs (default: \"localhost\"). Set to machine IP or \"host.containers.internal\" for Docker.",
          "type": "string"
        },
        "advertiseProtocol": {
          "description": "Protocol used in advertised URLs (default: \"http\"). Set to \"https\" when the agent is deployed behind TLS or a TLS-terminating reverse proxy. The A2A spec requires HTTPS for production deployments.",
          "enum": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "hostname": {
          "description": "Bind address (default: \"0.0.0.0\")",
          "type": "string"
        },
        "port": {
          "description": "A2A server port (default: 3000)",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SessionConfig": {
      "additionalProperties": false,
      "description": "Session lifecycle management.",
      "properties": {
        "cleanupInterval": {
          "description": "Session cleanup interval in ms (default: 300_000 = 5 min)",
          "type": "number"
        },
        "reuseByContext": {
          "description": "Reuse sessions by A2A contextId (default: true)",
          "type": "boolean"
        },
        "sessionMapFile": {
          "description": "Path to a JSON file for persisting the contextId→sessionId mapping across restarts. When set, the map is loaded on startup and written on every update. When absent, the mapping is in-memory only (default behavior).",
          "type": "string"
        },
        "titlePrefix": {
          "description": "Session title prefix (default: \"A2A Session\")",
          "type": "string"
        },
        "ttl": {
          "description": "Session TTL in ms (default: 3_600_000 = 1 hour)",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SkillConfig": {
      "additionalProperties": false,
      "description": "A single skill exposed on the agent card.",
      "properties": {
        "description": {
          "type": "string"
        },
        "examples": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "tags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ],
      "type": "object"
    },
    "TimeoutConfig": {
      "additionalProperties": false,
      "description": "Timeout settings.",
      "properties": {
        "healthCheck": {
          "description": "Health check interval in ms (default: 30_000). 0 to disable.",
          "type": "number"
        },
        "pollingInterval": {
          "description": "Polling interval in ms for fallback (default: 2_000)",
          "type": "number"
        },
        "prompt": {
          "description": "Timeout for a single prompt in ms (default: 300_000 = 5 min)",
          "type": "number"
        }
      },
      "type": "object"
    }
  }
}